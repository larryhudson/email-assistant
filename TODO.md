- agent skills - should be loaded by pydantic ai from 'skills' folder in workspace. this means if the agent creates skills for itself, they can be used in subsequent conversations. should also include simple skill telling how to create skills. pydantic ai should have a standard way to do this.
- markdown to html conversion when sending email back with mailgun
- mobile-optimised ui for agent admin
- more robust error handling. send an email when an exception is thrown, send it to both the end user and the admin user
- scheduled tasks - agent should have a tool where it can list / create / delete scheduled tasks. these should be able to be 'one off' or 'cron' which runs on an interval. basically when a task runs, it will inject a synthetic email message which will trigger the agent run
- CONTEXT.md file in workspace should be injected into the system prompt, and agent should be instructed to put core long term knowledge into this file (e.g. who is the user, user preferences for how the agent should work, etc.). maybe need another lightweight skill for how to manage the CONTEXT.md file
- inject the cost info into the email template when we send the email over mailgun, after a <hr /> divider. i think we might need a way for that to not leak into the email conversation when the user replies to an assistant email (e.g. it might think that the assistant 'said' the cost info etc.). so we probably need a way of getting the 'body' content of the email without getting the 'footer' which would be the cost info, maybe be a run id, maybe a link to the admin interface etc.
